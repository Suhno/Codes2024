Module HelloWorld
    Sub Main()
        ' Display message on the console
        Console.WriteLine("Hello, World!")
        
        ' Wait for user input before closing
        Console.ReadLine()
    End Sub
End Module
